Consulta executada no console do D1:

SELECT name, type
FROM sqlite_schema
WHERE name NOT LIKE 'sqlite_%'
ORDER BY type, name;

Resultado (nomes e tipos devolvidos):

name                        type
--------------------------  -----
oauth_transactions_expiry   index
sessions_expiry             index
oauth_transactions          table
sessions                    table
